# Business OS worker stub
#
# Future responsibilities:
# - document extraction jobs
# - connector/CRM sync
# - AI analysis fan-out
#
# Current vertical slice executes those steps synchronously in the API
# (see POST /api/v1/demo/run).
